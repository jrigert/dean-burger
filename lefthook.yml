pre-push:
  parallel: true
  commands:
    test:
      glob: "*.{js,ts,jsx,tsx}"
      run: npm run test:ci

pre-commit:
 parallel: true
 commands:
   eslint:
     glob: "*.{js,ts,jsx,tsx}"
     run: npm run lint
   checktypes:
     glob: "*.{js,ts,jsx,tsx}"
     run: npm run checktypes
